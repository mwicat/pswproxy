import unittest
import mox

class ReplacerTestCase(unittest.TestCase):

    def testHandling(self):
        resp_mock = mox.MockObject(HTTPResponse)
        resp_mock.getheader("Content-Type").AndReturn("text/html")
        mox.Replay(resp_mock)
        Replacer.replace(resp_mock)
